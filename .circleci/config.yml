version: 2.1

orbs:
  aws-cli: circleci/aws-cli@5.4.1
  slack: circleci/slack@6.1.3

jobs:
  build_and_deploy:
    docker:
      - image: cimg/node:24.14
    environment:
      AWS_REGION: us-west-2
      SNAPSHOT_PREFIX: visual-baselines
      S3_BUCKET: ualibraries-libapps-prod
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: "arn:aws:iam::822769020289:role/ualibraries-libapps-circleci-role-prod"
          region: ${AWS_REGION}
          profile_name: "OIDC-PROFILE"
          role_session_name: ${CIRCLE_JOB}
          session_duration: "1800"
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build project
          command: npm run build
      - run:
          name: Deploy dist and images to S3
          command: |
            aws s3 sync dist "s3://${S3_BUCKET}" --metadata-directive REPLACE --cache-control "max-age=7200, must-revalidate"
            aws s3 sync images "s3://${S3_BUCKET}/images" --delete --metadata-directive REPLACE --cache-control "max-age=7200, must-revalidate"
      - run:
          name: Install Playwright Chromium
          command: npx playwright install --with-deps chromium
      - run:
          name: Update visual baseline snapshots
          command: npm run test:visual:update
      - run:
          name: Upload updated snapshots to S3
          command: aws s3 sync tests/visual.spec.js-snapshots "s3://${S3_BUCKET}/${SNAPSHOT_PREFIX}" --delete
      - slack/notify:
          event: fail
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":x: Deployment failed for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }
      - slack/notify:
          event: pass
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":white_check_mark: Deployment succeeded for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }

  build_and_deploy_sandbox:
    docker:
      - image: cimg/node:24.14
    environment:
      AWS_REGION: us-west-2
      SNAPSHOT_PREFIX: visual-baselines
      S3_BUCKET: ualibraries-libapps-dev
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: "arn:aws:iam::822769020289:role/ualibraries-libapps-circleci-role-dev"
          region: ${AWS_REGION}
          profile_name: "OIDC-PROFILE"
          role_session_name: ${CIRCLE_JOB}
          session_duration: "1800"
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Build project
          command: npm run build
      - run:
          name: Deploy dist and images to S3
          command: |
            aws s3 sync dist "s3://${S3_BUCKET}" --metadata-directive REPLACE --cache-control "no-cache, must-revalidate"
            aws s3 sync images "s3://${S3_BUCKET}/images" --delete --metadata-directive REPLACE --cache-control "no-cache, must-revalidate"
      - slack/notify:
          event: fail
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":x: Deployment failed for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }
      - slack/notify:
          event: pass
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":white_check_mark: Deployment succeeded for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }

  run_visual_regression_tests:
    docker:
      - image: cimg/node:24.14
    environment:
      AWS_REGION: us-west-2
      S3_BUCKET: ualibraries-libapps-prod
      SNAPSHOT_PREFIX: visual-baselines
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: "arn:aws:iam::822769020289:role/ualibraries-libapps-circleci-role-prod"
          region: ${AWS_REGION}
          profile_name: "OIDC-PROFILE"
          role_session_name: ${CIRCLE_JOB}
          session_duration: "1800"
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Install Playwright Chromium
          command: npx playwright install --with-deps chromium
      - run:
          name: Download visual baseline snapshots from S3
          command: |
            mkdir -p tests/visual.spec.js-snapshots
            aws s3 sync "s3://${S3_BUCKET}/${SNAPSHOT_PREFIX}" tests/visual.spec.js-snapshots --delete
      - run:
          name: Run visual regression tests
          command: npm run test:visual
      - slack/notify:
          event: fail
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":x: Visual regression tests failed for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }
      - slack/notify:
          event: pass
          custom: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":white_check_mark: Visual regression tests passed for *${CIRCLE_PROJECT_REPONAME}*.\nBuild: ${CIRCLE_BUILD_URL}"
                  }
                }
              ]
            }
      - store_artifacts:
          path: playwright-report
          destination: playwright-report
      - store_artifacts:
          path: test-results
          destination: test-results

workflows:
  deploy_on_main:
    jobs:
      - run_visual_regression_tests:
          context:
            - slack-notification
          filters:
            branches:
              only:
                - main

      # Normal path: visual regression tests pass, deploy automatically.
      - build_and_deploy:
          context:
            - slack-notification
          name: build_and_deploy_auto
          requires:
            - run_visual_regression_tests:
                - success
          filters:
            branches:
              only:
                - main

      # Failure path: visual regression tests fail, require human approval.
      - approve_visual_regression_failure:
          type: approval
          requires:
            - run_visual_regression_tests:
                - failed
          filters:
            branches:
              only:
                - main

      # After approval, deploy anyway.
      - build_and_deploy:
          context:
            - slack-notification
          name: build_and_deploy_after_approval
          requires:
            - approve_visual_regression_failure
          filters:
            branches:
              only:
                - main

  deploy_to_sandbox:
    jobs:
      # Deploy to sandbox
      - build_and_deploy_sandbox:
          name: build_and_deploy_sandbox
          context:
            - slack-notification
          filters:
            branches:
              only:
                - develop

  daily_visual_regression:
    triggers:
      - schedule:
          cron: "0 12 * * *"
          filters:
            branches:
              only:
                - main
    jobs:
      - run_visual_regression_tests:
          context:
            - slack-notification
