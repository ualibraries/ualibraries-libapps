{
  "name": "ualibraries-libapps",
  "version": "1.0.0",
  "description": "University of Arizona Libraries customizations and workflows for Springshare/LibApps.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && aws s3 sync dist s3://${S3_BUCKET:-ualibraries-libapps-sandbox} && aws s3 sync images s3://${S3_BUCKET:-ualibraries-libapps-sandbox}/images",
    "test:visual": "playwright test tests/visual.spec.js",
    "test:visual:update": "playwright test tests/visual.spec.js --update-snapshots",
    "test:visual:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  }
}
